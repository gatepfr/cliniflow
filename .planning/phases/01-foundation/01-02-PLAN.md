---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - .env.example
  - .gitignore
  - .npmrc
autonomous: true
requirements:
  - FOUND-01

must_haves:
  truths:
    - "`docker compose up -d` starts all three services (postgres, redis, evolution-api) without configuration changes"
    - "Redis is configured with `maxmemory-policy noeviction` from the compose file"
    - "Evolution API uses image `evoapicloud/evolution-api:v2.3.7` (not `atendai` or `latest`)"
    - "`.env.example` documents every required env var with safe placeholder values"
    - "`.gitignore` prevents `.env` from being committed"
  artifacts:
    - path: "docker-compose.yml"
      provides: "Three-service dev environment: postgres:16, redis:7-alpine, evolution-api:v2.3.7"
      contains: "evoapicloud/evolution-api:v2.3.7"
    - path: ".env.example"
      provides: "Template for all environment variables required by the monorepo"
      contains: "DATABASE_URL"
    - path: ".gitignore"
      provides: "Prevents secrets and build artifacts from being committed"
      contains: ".env"
    - path: ".npmrc"
      provides: "pnpm workspace hoisting config"
      contains: "shamefully-hoist"
  key_links:
    - from: "docker-compose.yml redis service"
      to: "Redis maxmemory policy"
      via: "command: redis-server --maxmemory-policy noeviction"
      pattern: "maxmemory-policy noeviction"
    - from: "docker-compose.yml evolution-api"
      to: "EVOLUTION_API_KEY env var"
      via: "AUTHENTICATION_API_KEY: ${EVOLUTION_API_KEY:-changeme-dev-key}"
      pattern: "AUTHENTICATION_API_KEY"
    - from: ".env.example"
      to: "apps/api DATABASE_URL"
      via: "postgresql://clinica:changeme@localhost:5432/clinicaflow"
      pattern: "DATABASE_URL=postgresql://"
---

<objective>
Create the local development infrastructure: `docker-compose.yml` with PostgreSQL 16, Redis 7, and Evolution API v2.3.7 pre-configured; `.env.example` with all required variables; and ancillary root config files (`.gitignore`, `.npmrc`).

Purpose: Satisfies D-06 — `docker compose up` must bring up a complete dev environment with zero manual configuration. This plan is parallel to Plan 01 (no file overlap).
Output: A fully functional `docker compose up -d` environment ready for subsequent plans.
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/projetos/saas-clinicas/.planning/PROJECT.md
@C:/projetos/saas-clinicas/.planning/ROADMAP.md
@C:/projetos/saas-clinicas/CLAUDE.md
@C:/projetos/saas-clinicas/.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: docker-compose.yml — PostgreSQL 16, Redis 7 (noeviction), Evolution API v2.3.7</name>
  <read_first>
    - C:/projetos/saas-clinicas/.planning/phases/01-foundation/01-RESEARCH.md (section "Evolution API — Versão e Configuração Docker" — docker-compose.yml template)
    - C:/projetos/saas-clinicas/CLAUDE.md (section 8.1 — Evolution API setup, section 3 — stack versions)
  </read_first>
  <files>docker-compose.yml</files>
  <action>
Create `docker-compose.yml` at project root. Use the exact image tag `evoapicloud/evolution-api:v2.3.7` (NOT `atendai/evolution-api` which is v1 only, NOT `latest` which is RC).

CRITICAL: Redis `command: redis-server --maxmemory-policy noeviction` is mandatory — default `allkeys-lru` silently evicts BullMQ keys causing duplicate campaign sends and WhatsApp ban risk.

```yaml
version: "3.9"

services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_DB: clinicaflow
      POSTGRES_USER: ${POSTGRES_USER:-clinica}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-changeme}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-clinica} -d clinicaflow"]
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    command: redis-server --maxmemory-policy noeviction
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10

  evolution-api:
    image: evoapicloud/evolution-api:v2.3.7
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    ports:
      - "8080:8080"
    environment:
      SERVER_URL: http://localhost:8080
      AUTHENTICATION_API_KEY: ${EVOLUTION_API_KEY:-changeme-dev-key}
      DATABASE_ENABLED: "true"
      DATABASE_PROVIDER: postgresql
      DATABASE_CONNECTION_URI: postgresql://${POSTGRES_USER:-clinica}:${POSTGRES_PASSWORD:-changeme}@postgres:5432/evolution_api?schema=evolution_api
      CACHE_REDIS_ENABLED: "true"
      CACHE_REDIS_URI: redis://redis:6379/1
      CACHE_REDIS_PREFIX_KEY: evolution
      WEBHOOK_GLOBAL_URL: ""
      LOG_LEVEL: ERROR
    volumes:
      - evolution_instances:/evolution/instances

volumes:
  postgres_data:
  redis_data:
  evolution_instances:
```

Note: The Evolution API uses a SEPARATE PostgreSQL database (`evolution_api` schema) from the main application database (`clinicaflow`). Both share the same PostgreSQL container but different databases/schemas.
  </action>
  <verify>
    <automated>cd C:/projetos/saas-clinicas && grep "evoapicloud/evolution-api:v2.3.7" docker-compose.yml && grep "maxmemory-policy noeviction" docker-compose.yml && grep "postgres:16" docker-compose.yml && grep "redis:7-alpine" docker-compose.yml</automated>
  </verify>
  <acceptance_criteria>
    - `docker-compose.yml` contains `evoapicloud/evolution-api:v2.3.7` (exact tag, exact org)
    - `docker-compose.yml` contains `redis-server --maxmemory-policy noeviction` in redis command
    - `docker-compose.yml` contains `postgres:16` image
    - `docker-compose.yml` contains `redis:7-alpine` image
    - `docker-compose.yml` evolution-api service has `AUTHENTICATION_API_KEY: ${EVOLUTION_API_KEY:-changeme-dev-key}`
    - `docker-compose.yml` has `depends_on` with health conditions for evolution-api waiting on postgres and redis
    - `docker-compose.yml` declares three named volumes: `postgres_data`, `redis_data`, `evolution_instances`
  </acceptance_criteria>
  <done>`docker-compose.yml` exists at project root and all service configurations match the spec above.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: .env.example, .gitignore, .npmrc — environment template and root config</name>
  <read_first>
    - C:/projetos/saas-clinicas/.planning/phases/01-foundation/01-RESEARCH.md (section "Variáveis de Ambiente Required (.env.example)")
    - C:/projetos/saas-clinicas/CLAUDE.md (section 2.1 — LGPD, never log PII; section 6 — conventions)
  </read_first>
  <files>.env.example, .gitignore, .npmrc</files>
  <action>
**`.env.example`** — document every env var the monorepo needs. Use placeholder values that will not work in production but are safe to commit:

```bash
# ============================================================
# ClínicaFlow — Environment Variables
# Copy to .env and fill in real values. NEVER commit .env.
# ============================================================

# --- PostgreSQL ---
POSTGRES_USER=clinica
POSTGRES_PASSWORD=changeme
DATABASE_URL=postgresql://clinica:changeme@localhost:5432/clinicaflow

# --- Redis ---
REDIS_HOST=localhost
REDIS_PORT=6379

# --- JWT ---
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_ACCESS_SECRET=change-this-secret-in-production-min-32-chars
JWT_REFRESH_SECRET=change-this-other-secret-in-production-min-32-chars
JWT_ACCESS_TTL=15m

# --- Evolution API (WhatsApp) ---
EVOLUTION_API_URL=http://localhost:8080
EVOLUTION_API_KEY=changeme-dev-key
# EVOLUTION_INSTANCE_NAME is set per tenant at runtime — not a global env var

# --- Sentry (leave empty in development) ---
SENTRY_DSN=

# --- App ---
NODE_ENV=development
LOG_LEVEL=info
PORT=3000
```

**`.gitignore`**:
```gitignore
# Environment
.env
.env.local
.env.*.local

# Dependencies
node_modules/
.pnpm-store/

# Build outputs
dist/
build/
.next/
out/

# Turbo
.turbo/

# TypeScript
*.tsbuildinfo

# Prisma
prisma/migrations/dev_*

# OS
.DS_Store
Thumbs.db

# Logs
*.log
logs/

# Test coverage
coverage/
.nyc_output/

# IDE
.vscode/
.idea/
*.swp
*.swo
```

**`.npmrc`** (pnpm config):
```ini
shamefully-hoist=true
strict-peer-dependencies=false
```

`shamefully-hoist=true` is required for Turborepo + shadcn/ui to resolve their peer dependencies correctly in a pnpm workspace. `strict-peer-dependencies=false` avoids CI failures from peer dep warnings (React 19 + shadcn compatibility).
  </action>
  <verify>
    <automated>cd C:/projetos/saas-clinicas && grep "DATABASE_URL=postgresql://" .env.example && grep "JWT_ACCESS_SECRET" .env.example && grep "EVOLUTION_API_KEY" .env.example && grep "^\.env$" .gitignore && grep "node_modules" .gitignore && grep "shamefully-hoist" .npmrc</automated>
  </verify>
  <acceptance_criteria>
    - `.env.example` contains `DATABASE_URL=postgresql://clinica:changeme@localhost:5432/clinicaflow`
    - `.env.example` contains `JWT_ACCESS_SECRET`, `JWT_REFRESH_SECRET`, `JWT_ACCESS_TTL=15m`
    - `.env.example` contains `EVOLUTION_API_URL` and `EVOLUTION_API_KEY`
    - `.env.example` contains `SENTRY_DSN=` (empty, not missing)
    - `.gitignore` has `.env` as a standalone line (exact match, not `.env.example`)
    - `.gitignore` has `node_modules/` and `dist/`
    - `.npmrc` has `shamefully-hoist=true` and `strict-peer-dependencies=false`
    - `.env` file does NOT exist in the repository (not created, only `.env.example`)
  </acceptance_criteria>
  <done>`.env.example` documents all 9 required env vars; `.gitignore` prevents `.env` from being committed; `.npmrc` enables workspace hoisting for Turborepo and shadcn compatibility.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Docker network | Containers communicate on internal Docker bridge; only published ports (5432, 6379, 8080) are accessible from host |
| Env var secrets | `.env` contains secrets; must never be committed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-PLAN02-01 | Information Disclosure | `.env` file committed to git | mitigate | `.gitignore` lists `.env` as exact-match entry; `.env.example` uses placeholder values only |
| T-1-PLAN02-02 | Spoofing | Evolution API `AUTHENTICATION_API_KEY` default value | mitigate | Default `changeme-dev-key` is only for local dev; `.env.example` comment instructs rotation before any real WhatsApp connection. Production must override via real env var. |
| T-1-PLAN02-03 | Denial of Service | Redis memory exhaustion evicting BullMQ keys | mitigate | `maxmemory-policy noeviction` in docker-compose.yml ensures Redis returns OOM error instead of silent eviction — campaign jobs are never silently lost |
| T-1-PLAN02-04 | Tampering | PostgreSQL port 5432 exposed on all interfaces | accept | Development only; production Hetzner uses private network with no public port exposure. Document in `.env.example` that Docker ports are dev-only. |
</threat_model>

<verification>
After completing both tasks:

1. `grep "evoapicloud/evolution-api:v2.3.7" docker-compose.yml` — returns the image line
2. `grep "maxmemory-policy noeviction" docker-compose.yml` — returns the redis command line
3. `grep "\.env" .gitignore` — returns `.env` entry
4. `cat .env.example | grep -c "="` — shows at least 9 env var definitions
5. `cat .npmrc` — shows `shamefully-hoist=true`
6. Manual: `docker compose up -d` starts without error (requires Docker running)
</verification>

<success_criteria>
- `docker-compose.yml` works with `docker compose up -d` — zero manual config needed (D-06)
- Redis `maxmemory-policy noeviction` prevents silent BullMQ key eviction (Pitfall 3 from RESEARCH.md)
- Evolution API uses `evoapicloud/evolution-api:v2.3.7` (D-07 — confirmed correct org/tag)
- `.env.example` provides all 9 env vars with placeholder values and inline comments
- `.gitignore` prevents `.env`, `node_modules/`, `dist/`, `.turbo/` from being committed
- FOUND-01 partially satisfied (infrastructure scaffold complete; app startup tested in Plan 09)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
