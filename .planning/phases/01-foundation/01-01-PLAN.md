---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - turbo.json
  - tsconfig.base.json
  - apps/api/package.json
  - apps/api/tsconfig.json
  - apps/web/package.json
  - apps/web/tsconfig.json
  - apps/worker/package.json
  - apps/worker/tsconfig.json
  - packages/db/package.json
  - packages/db/tsconfig.json
  - packages/shared/package.json
  - packages/shared/tsconfig.json
  - packages/ai/package.json
  - packages/ai/tsconfig.json
  - packages/whatsapp/package.json
  - packages/whatsapp/tsconfig.json
  - packages/billing/package.json
  - packages/billing/tsconfig.json
autonomous: true
requirements:
  - FOUND-01

must_haves:
  truths:
    - "`pnpm install` completes at monorepo root with no workspace resolution errors"
    - "`turbo run build` executes the build pipeline and resolves inter-package dependencies"
    - "Each app/package has its own package.json with correct `name`, `exports`, and workspace-relative dependencies"
    - "TypeScript `strict` mode is enabled in all packages via shared base config"
    - "`apps/ai`, `apps/billing` exist as stub packages with minimal package.json (no code yet)"
  artifacts:
    - path: "package.json"
      provides: "Root workspace config with turbo + typescript devDependencies"
      contains: "\"workspaces\""
    - path: "pnpm-workspace.yaml"
      provides: "Workspace glob declarations"
      contains: "apps/*"
    - path: "turbo.json"
      provides: "Task pipeline config for build/dev/test/lint"
      contains: "\"tasks\""
    - path: "tsconfig.base.json"
      provides: "Shared TypeScript strict config extended by all packages"
      contains: "\"strict\": true"
    - path: "apps/api/package.json"
      provides: "API app package with Fastify and workspace dependencies"
      contains: "@clinicaflow/db"
    - path: "packages/db/package.json"
      provides: "DB package with Prisma"
      contains: "@clinicaflow/db"
  key_links:
    - from: "apps/api/package.json"
      to: "packages/db"
      via: "workspace:* dependency"
      pattern: "\"@clinicaflow/db\": \"workspace:\\*\""
    - from: "apps/api/package.json"
      to: "packages/shared"
      via: "workspace:* dependency"
      pattern: "\"@clinicaflow/shared\": \"workspace:\\*\""
    - from: "turbo.json"
      to: "package.json scripts"
      via: "turbo run dev/build/test"
      pattern: "\"dev\": \"turbo run dev\""
---

<objective>
Create the complete monorepo skeleton: root workspace config, Turborepo pipeline, shared TypeScript config, and package.json files for all 8 apps/packages with correct naming, exports, and inter-package dependency declarations.

Purpose: Establishes the structural foundation that all subsequent plans build on. No app or package can be implemented until the workspace graph is correctly wired.
Output: A working `pnpm install` + `turbo run build` monorepo with all 8 packages present (6 real + 2 stubs).
</objective>

<execution_context>
@C:/Users/User/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/projetos/saas-clinicas/.planning/PROJECT.md
@C:/projetos/saas-clinicas/.planning/ROADMAP.md
@C:/projetos/saas-clinicas/.planning/STATE.md
@C:/projetos/saas-clinicas/CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Root workspace files — package.json, pnpm-workspace.yaml, turbo.json, tsconfig.base.json</name>
  <read_first>
    - C:/projetos/saas-clinicas/package.json (if exists — read current state before overwriting)
    - C:/projetos/saas-clinicas/CLAUDE.md (stack versions, packageManager field)
    - C:/projetos/saas-clinicas/.planning/phases/01-foundation/01-RESEARCH.md (Pattern 1 — Turborepo Pipeline Configuration)
  </read_first>
  <files>
    package.json,
    pnpm-workspace.yaml,
    turbo.json,
    tsconfig.base.json
  </files>
  <action>
Create four root-level files.

**`package.json`** (root, private monorepo):
```json
{
  "name": "clinicaflow",
  "private": true,
  "packageManager": "pnpm@10.33.2",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "turbo": "2.9.14",
    "typescript": "^5"
  }
}
```

**`pnpm-workspace.yaml`**:
```yaml
packages:
  - "apps/*"
  - "packages/*"
```

**`turbo.json`**:
```jsonc
{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "test": {
      "dependsOn": ["^build"],
      "outputs": ["coverage/**"]
    },
    "lint": {
      "dependsOn": ["^build"]
    },
    "typecheck": {
      "dependsOn": ["^build"]
    }
  }
}
```

**`tsconfig.base.json`** (shared strict config):
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "lib": ["ES2022"],
    "strict": true,
    "exactOptionalPropertyTypes": true,
    "noUncheckedIndexedAccess": true,
    "noImplicitOverride": true,
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true,
    "outDir": "dist",
    "rootDir": "src",
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true
  }
}
```
  </action>
  <verify>
    <automated>cd C:/projetos/saas-clinicas && cat package.json | grep -E '"private"|"packageManager"|"turbo"' && cat pnpm-workspace.yaml | grep "apps/\*" && cat turbo.json | grep '"tasks"' && cat tsconfig.base.json | grep '"strict"'</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains `"private": true`, `"packageManager": "pnpm@10.33.2"`, and `"turbo": "2.9.14"` in devDependencies
    - `pnpm-workspace.yaml` contains `"apps/*"` and `"packages/*"` under `packages:`
    - `turbo.json` contains `"tasks"` key with `build`, `dev`, `test`, `lint`, `typecheck`
    - `tsconfig.base.json` contains `"strict": true` and `"module": "NodeNext"`
  </acceptance_criteria>
  <done>Four root configuration files exist and contain all required fields as specified above.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: All 8 app and package manifests — package.json + tsconfig.json for every workspace member</name>
  <read_first>
    - C:/projetos/saas-clinicas/.planning/phases/01-foundation/01-RESEARCH.md (Pattern 1 — inter-package dependencies, Standard Stack versions, Installation commands)
    - C:/projetos/saas-clinicas/CLAUDE.md (section 4.2 — Camadas, package names)
  </read_first>
  <files>
    apps/api/package.json,
    apps/api/tsconfig.json,
    apps/web/package.json,
    apps/web/tsconfig.json,
    apps/worker/package.json,
    apps/worker/tsconfig.json,
    packages/db/package.json,
    packages/db/tsconfig.json,
    packages/shared/package.json,
    packages/shared/tsconfig.json,
    packages/ai/package.json,
    packages/ai/tsconfig.json,
    packages/whatsapp/package.json,
    packages/whatsapp/tsconfig.json,
    packages/billing/package.json,
    packages/billing/tsconfig.json
  </files>
  <action>
Create directory structure and all package manifests. Create each directory if it does not exist.

**`apps/api/package.json`**:
```json
{
  "name": "@clinicaflow/api",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@clinicaflow/db": "workspace:*",
    "@clinicaflow/shared": "workspace:*",
    "@clinicaflow/whatsapp": "workspace:*",
    "@fastify/cookie": "11.0.2",
    "@fastify/cors": "11.2.0",
    "@fastify/jwt": "10.1.0",
    "@fastify/rate-limit": "10.3.0",
    "@sentry/node": "10.53.1",
    "fastify": "5.8.5",
    "pino": "10.3.1",
    "pino-pretty": "13.1.3"
  },
  "devDependencies": {
    "@types/node": "^20",
    "tsx": "latest",
    "typescript": "^5",
    "vitest": "4.1.7"
  }
}
```

**`apps/api/tsconfig.json`**:
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "dist",
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"]
}
```

**`apps/web/package.json`**:
```json
{
  "name": "@clinicaflow/web",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@clinicaflow/shared": "workspace:*",
    "react": "^19",
    "react-dom": "^19",
    "tailwindcss": "^4"
  },
  "devDependencies": {
    "@tailwindcss/vite": "latest",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "@vitejs/plugin-react": "latest",
    "typescript": "^5",
    "vite": "latest"
  }
}
```

**`apps/web/tsconfig.json`**:
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "jsx": "react-jsx",
    "baseUrl": ".",
    "paths": { "@/*": ["./src/*"] },
    "declaration": false,
    "skipLibCheck": true,
    "esModuleInterop": true
  },
  "include": ["src/**/*", "vite.config.ts"],
  "exclude": ["node_modules", "dist"]
}
```

**`apps/worker/package.json`**:
```json
{
  "name": "@clinicaflow/worker",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@clinicaflow/db": "workspace:*",
    "@clinicaflow/shared": "workspace:*",
    "@clinicaflow/whatsapp": "workspace:*",
    "@sentry/node": "10.53.1",
    "bullmq": "5.77.1",
    "ioredis": "5.10.1",
    "pino": "10.3.1"
  },
  "devDependencies": {
    "@types/node": "^20",
    "tsx": "latest",
    "typescript": "^5",
    "vitest": "4.1.7"
  }
}
```

**`apps/worker/tsconfig.json`**:
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "dist"
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"]
}
```

**`packages/db/package.json`**:
```json
{
  "name": "@clinicaflow/db",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:push": "prisma db push"
  },
  "dependencies": {
    "@paralleldrive/cuid2": "3.3.0",
    "@prisma/client": "7.8.0",
    "prisma": "7.8.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5",
    "vitest": "4.1.7"
  }
}
```

**`packages/db/tsconfig.json`**:
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "dist"
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"]
}
```

**`packages/shared/package.json`**:
```json
{
  "name": "@clinicaflow/shared",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@paralleldrive/cuid2": "3.3.0",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5"
  }
}
```

**`packages/shared/tsconfig.json`**:
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "dist"
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"]
}
```

**`packages/ai/package.json`** (stub — no code yet):
```json
{
  "name": "@clinicaflow/ai",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5"
  }
}
```

**`packages/ai/tsconfig.json`**:
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "dist"
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"]
}
```

**`packages/whatsapp/package.json`**:
```json
{
  "name": "@clinicaflow/whatsapp",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@clinicaflow/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5",
    "vitest": "4.1.7"
  }
}
```

**`packages/whatsapp/tsconfig.json`**:
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "dist"
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"]
}
```

**`packages/billing/package.json`** (stub — no code yet):
```json
{
  "name": "@clinicaflow/billing",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20",
    "typescript": "^5"
  }
}
```

**`packages/billing/tsconfig.json`**:
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "dist"
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"]
}
```

After creating all files, create stub `src/index.ts` files for packages that declare exports (db, shared, ai, whatsapp, billing) so `tsc` does not fail on empty `src/`:
- `packages/db/src/index.ts` → `export {};`
- `packages/shared/src/index.ts` → `export {};`
- `packages/ai/src/index.ts` → `export {};`
- `packages/whatsapp/src/index.ts` → `export {};`
- `packages/billing/src/index.ts` → `export {};`

Also create `apps/api/src/server.ts` stub → `export {};` and `apps/worker/src/index.ts` stub → `export {};` so `tsc` does not fail.

Run `pnpm install` from the monorepo root after all files are created.
  </action>
  <verify>
    <automated>cd C:/projetos/saas-clinicas && cat apps/api/package.json | grep '"@clinicaflow/db"' && cat packages/db/package.json | grep '"@clinicaflow/db"' && cat packages/shared/package.json | grep '"zod"' && cat packages/whatsapp/package.json | grep '"@clinicaflow/shared"' && ls packages/ai/package.json && ls packages/billing/package.json</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/package.json` contains `"@clinicaflow/db": "workspace:*"` and `"@clinicaflow/shared": "workspace:*"`
    - `apps/worker/package.json` contains `"bullmq": "5.77.1"` and `"ioredis": "5.10.1"`
    - `packages/db/package.json` name is `"@clinicaflow/db"` and has `@paralleldrive/cuid2` and `prisma`
    - `packages/shared/package.json` contains `"zod": "4.4.3"`
    - `packages/whatsapp/package.json` depends on `"@clinicaflow/shared": "workspace:*"`
    - `packages/ai/package.json` and `packages/billing/package.json` exist (stubs)
    - All 8 package.json files contain `"type": "module"`
    - All tsconfig.json files extend `"../../tsconfig.base.json"` (except apps/web which uses bundler mode)
    - `pnpm install` completes without error at monorepo root
  </acceptance_criteria>
  <done>All 8 workspace members have package.json + tsconfig.json. `pnpm install` resolves all workspace dependencies without errors. Stub src/index.ts files exist in all packages with exports declarations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build toolchain | turbo.json and package.json scripts run locally and in CI; malicious dependency injection is the main concern |
| Workspace links | `workspace:*` pins to local code; prevents supply-chain substitution at this layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-PLAN01-01 | Tampering | package.json dependency versions | mitigate | Pin all production deps to exact versions (not ranges); only devDependencies use `^` ranges. Verified versions from research: fastify@5.8.5, bullmq@5.77.1, etc. |
| T-1-PLAN01-02 | Information Disclosure | turbo cache | accept | Turbo local cache contains build artifacts, not secrets. Remote cache disabled by default. Low risk in Phase 1. |
</threat_model>

<verification>
After completing both tasks:

1. `cat pnpm-workspace.yaml` shows `"apps/*"` and `"packages/*"`
2. `cat turbo.json` contains all 5 task types (build, dev, test, lint, typecheck)
3. `cat tsconfig.base.json` contains `"strict": true`
4. `cat apps/api/package.json` shows `"@clinicaflow/db": "workspace:*"` dependency
5. `cat packages/db/package.json` name is `"@clinicaflow/db"` with prisma + cuid2
6. All 8 workspace directories exist: `apps/api`, `apps/web`, `apps/worker`, `packages/db`, `packages/shared`, `packages/ai`, `packages/whatsapp`, `packages/billing`
7. `pnpm install` (from root) resolves without errors
</verification>

<success_criteria>
- 8 workspace packages exist with correct `name` field (`@clinicaflow/*`)
- All inter-package dependencies use `workspace:*` protocol
- `turbo.json` pipeline: build depends on `^build`, dev is persistent + no-cache
- `tsconfig.base.json` enables `strict`, `exactOptionalPropertyTypes`, `noUncheckedIndexedAccess`
- `pnpm install` completes clean at monorepo root
- FOUND-01 partially satisfied (workspace scaffold complete; apps implement in Plans 06-08)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
